# Agentic loop — runtime artifacts only (the .agent/*.js scripts ARE tracked)
# .agent/screenshots/
# .agent/logs/
# .agent/run_*.json
# .agent/apk_size_baseline.txt
# .agentrc.local
node_modules/
# package-lock.json
# .DS_Store
# Secrets
.env
